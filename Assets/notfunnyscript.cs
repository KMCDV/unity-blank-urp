using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class notfunnyscript : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //its not funny script
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
